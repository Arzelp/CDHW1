#ifndef __ERROR_H__
#define __ERROR_H__

#define NULL_MALLOC "Failed to allocate the desired memory\n"
#define NULL_MEMSET "Failed to fill the array using memset\n"
#define INVALID_INPUT "The size and the max element must be positives\n"
#define INVALID_MAX_ELEM "The maximum element is false (the software found a greater one)\n"

#endif
